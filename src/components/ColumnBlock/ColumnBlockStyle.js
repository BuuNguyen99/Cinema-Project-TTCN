const styles = (theme) => ({
    wrap: {
        width: '100%',
        padding: 10
    },
    header: {
        backgroundColor: theme.color.orange,
        padding: '10px 20px'
    }
  });
  
  export default styles;
  